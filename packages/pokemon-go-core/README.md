# `@pokeyou/pokemon-go-core`

Shared TypeScript logic for the Pokémon GO collector used by:

- Next.js web app (repo root)
- Expo mobile app (`apps/mobile`)

Includes types, engine, evolutions, goals, cloud-sync helpers, import/export parsers, and bundled master JSON under `data/`.
